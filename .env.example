# Copy this file to .env and set your backend URL.
# Do not commit .env — it is listed in .gitignore.
#
# Local dev (direct to backend):
VITE_API_BASE=http://localhost:5000/todos
#
# Vercel deploy — use relative path (vercel.json proxy), NOT full http:// URL:
# VITE_API_BASE=/api/todos
